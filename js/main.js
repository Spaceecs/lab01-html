document.addEventListener("DOMContentLoaded", init);

function init() {
  initActiveNav();
  initMenuToggle();
  initThemeToggle();
  initBackToTopAndYear();

  initAccordion();
  initFilters();
  initModal();
  initContactForm();
}
